<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Crolow.Designer.Wpf.App.Views.Document.Canvas.SelectionEditors">

    <SolidColorBrush x:Key="DialogBackgroundBrush" Color="#2B2B2B"/>
    <SolidColorBrush x:Key="DialogForegroundBrush" Color="White"/>
    <SolidColorBrush x:Key="DialogBorderBrush" Color="#555"/>

    <!-- Window -->
    <Style TargetType="{x:Type Window}">
        <Setter Property="Background" Value="{StaticResource DialogBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource DialogForegroundBrush}"/>
        <Setter Property="FontSize" Value="13"/>
    </Style>

    <!-- Labels -->
    <Style TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="{StaticResource DialogForegroundBrush}"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <!-- TextBox -->
    <Style TargetType="{x:Type TextBox}">
        <Setter Property="Background" Value="#3A3A3A"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderBrush" Value="#666"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="6,3"/>
    </Style>

    <!-- Button -->
    <Style TargetType="{x:Type Button}">
        <Setter Property="Background" Value="#404040"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderBrush" Value="#666"/>
        <Setter Property="Padding" Value="20,6"/>
    </Style>

    <Style TargetType="{x:Type controls:SelectionTransformController}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="HandleSize" Value="10" />
        <Setter Property="RotationHandleDistance" Value="24" />
        <Setter Property="MinWidth" Value="4" />
        <Setter Property="MinHeight" Value="4" />
    </Style>
</ResourceDictionary>